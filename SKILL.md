---
name: gongwen-gems
description: "为党政公文写作提供修辞触发器（逻辑骨架，非成品句）。写工作总结/领导讲话稿/汇报材料/调研报告/述职/事迹材料需要点睛句、过渡句或破题句时使用；强制按三步转化法填入用户真实信息，禁止直接引用、禁止空话。不用于对话闲聊。"
metadata:
  version: 3.0.0
license: CC-BY-NC-SA-4.0
---

# Gongwen Gems — 修辞触发器（v3）

本库不是成品句库，是**修辞触发器**：每条只提供**逻辑骨架**，用来启发思路、强制转化。所有句子都必须经「三步转化法」用用户自己的实词重组后才可使用。

> 句库全文见 [`references/gems.md`](references/gems.md)，使用本技能前必须读取。
> 完整范例见 [`examples/`](examples/)。

## 定位

- **不是**：拿来即用的好句集合。
- **是**：逼迫你把空话换成实词的转化装置。借用其逻辑骨架，填入你的任务、数据、主体、时限。

## 核心原则

1. **不禁止引用，只要求转化。** 没有"绝对禁用"的句子，只有未完成转化的引用。
2. **不限制数量，只规定配比。** 不看全文用了几句，只看每处引用所在段落的事实密度。
3. **不强记文种，只训练逻辑。** 不背"请示禁用什么"，而是判断当前语境需要"定调/剖析/执行/收束"哪种功能。

## 三大使用定律

### 定律一：虚实配重比（1:2）

每引用本库一句，**所在自然段内必须至少含 2 处具体事实**（数据、案例、举措、时限）。

- 事实可以是：数字、单位名、人名、文件名、日期、项目名、事件。
- 引用句本身被填实的占位符算 1 处事实，段落内还须另有至少 1 处独立事实。
- 段落内凑不齐 2 处事实 → 放弃该句，不要降低事实标准。

**示例（引用"挂图作战"，段内 2 处事实 = 540 天工期 + 15 天超时序）：**
> 对 9 个在建项目挂图作战：人民医院新院区倒排 540 天工期，目前主体封顶，超时序 15 天。

### 定律二：语境降噪法

按文种"硬核程度"决定调用类型与表达强度。硬核程度 = 该文种离"办事依据"有多近（请示、批复、函 > 报告、总结 > 讲话稿、动员稿）。

| 文种 | 允许调用的类型 | 表达强度 |
|---|---|---|
| 请示 / 批复 / 函 / 通知 | 仅 C 措施执行型 + E 固定表述 | 降为平实陈述，去比喻、去对仗 |
| 报告 / 总结 / 调研报告 / 述职 | A/B/C 均可，D 慎用 | 陈述为主，可保留一句定调 |
| 讲话稿 / 动员稿 / 事迹材料 | A/B/C/D 均可 | 可保留修辞强度 |

- 请示里写"喊破嗓子不如甩开膀子"= 噪音；请示里写"针对 37 项整改任务逐项建台账、9 月 30 日前销号"= 转化成功。
- 判断标准：把句子删掉后，**办事依据（谁、做什么、何时、何标准）是否仍完整**。完整 → 该句是噪音，删除。

### 定律三：不可替代性检验

引用前做一次删除测试：**删掉该句，所在段落的表态功能是否受损？**

- 删除后核心信息（任务、数据、安排）仍完整 → 该句**自动删除，无需引用**。它只是装饰。
- 删除后段落只剩干巴巴的事实、缺少判断或态度 → 保留，这是它存在的理由。

**示例：**
> ❌ 段落：全局上下团结一心，今年完成投资 18.6 亿元。（删掉"团结一心"，信息无损 → 删）
> ✅ 段落：今年完成投资 18.6 亿元。这个数字来之不易，是拼出来的、干出来的。（删掉第二句，评价缺失 → 保留）

## 标准作业流程（必须按顺序）

### Step 1 提取"实料"

从用户输入中抓取 **3–5 个核心实词**：具体任务、量化指标、责任主体、时间节点、真实案例。没有这些 → 先向用户要，不得用句子凑。

### Step 2 定向"借壳"

按当前语境判断需要的修辞功能（A 破题定调 / B 问题剖析 / C 措施执行 / D 收束升华），从库中选**逻辑骨架**。只借骨架，不借词汇——骨架里的比喻、对仗词全部作废，一个都不保留。

### Step 3 重组"血肉"

把 Step 1 的实词全部填入骨架，替换掉所有原词与占位符。**填完不通顺 → 放弃该句，换一句或不用**，绝不硬凑。

> 转化不通的三种常见情形：① 实词与骨架逻辑冲突（如"制度执行"骨架配"防汛"实料）；② 实词不足，骨架撑不起；③ 填实后句子比原文还长还绕。三者任一 → 弃用。

## 跨文种变形示例

同一逻辑骨架（"一分部署，九分落实"），在不同文种下的正确转化：

| 文种 | 转化结果 |
|---|---|
| 动员讲话稿 | 一分部署，九分落实。42 项重点任务实行"月调度、季评估"，3 项滞后任务挂牌督办。 |
| 工作总结 | 坚持一分部署、九分落实，全年 42 项重点任务办结 39 项，办结率 92.9%。 |
| 请示 | 为落实市委"一分部署、九分落实"要求，我局拟对 42 项重点任务实行月度调度，请示所需经费 80 万元。妥否，请批示。 |
| 通知 | 各单位要对照任务清单逐项落实，9 月 30 日前将落实情况报局办公室。 |

注意变形规则：**文种越硬，骨架越隐**——请示、通知里不出现原句的对仗形态，只保留"部署必须落实"这一逻辑。

## 正反例

**❌ 直接引用（禁止）：**"我们要不忘初心、牢记使命，真抓实干、攻坚克难，以钉钉子精神抓好落实。"（四词堆砌，零实词）

**✅ 转化引用：**"以钉钉子精神推进 18 个城中村改造，一锤接着一锤敲：一季度签约 1200 户，二季度拆除 800 户。"（骨架 + 任务 + 节奏 + 数据）

**❌ 空占位符（禁止）：**"针对{具体问题}，我们建立台账、对账销号。"（花括号未填实）

**✅ 填实转化：**"对巡察反馈的 47 个问题建立台账、对账销号，已办结 41 个，办结率 87%。"

## 输出前自检（逐项打勾）

- [ ] 是否完成了 Step 1–3？（未提取实词 → 回去重做）
- [ ] 借的是骨架还是句子？（原样保留比喻/对仗词 → 未完成转化）
- [ ] 所在段落是否满足 1:2 配重比？（不足 2 处事实 → 删句或补事实）
- [ ] 该文种是否允许当前表达强度？（请示/通知出现修辞句 → 降为平实陈述）
- [ ] 删掉这句，段落是否依然完整？（完整 → 自动删除）
- [ ] 数据是否全部来自用户材料？（有编造 → 删除并索要素材）

## 免责提示

本库仅为辅助灵感与转化训练，最终文稿必须结合本地实际情况。建议用户提供更多一手素材（具体事例、数据、举措、时间节点），以确保文稿有血有肉、可落地、可核查。本库不承担代拟正式公文的责任。

## 分类速查（按修辞功能）

| 类 | 功能 | 句数 | 典型位置 |
|---|---|---|---|
| A 破题定调 | 开篇点题、定基调 | 7 | 篇首 |
| B 问题剖析 | 指出矛盾、点差距 | 10 | 问题段 |
| C 措施执行 | 部署落实 | 24 | 对策段 |
| C2 成效印证 | 措施的结果，须有真实数据 | 4 | 成效段 |
| D 收束升华 | 结尾鼓劲、表态承诺 | 11 | 篇尾/表态段 |
| E 固定表述 | 格式惯例，按规范直接使用 | 12 | 法定公文固定位置 |
