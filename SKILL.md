---
name: gongwen-gems
description: "为党政公文写作提供可落地的句式与好句。写工作总结/领导讲话稿/汇报材料/调研报告/述职/事迹材料需要点睛句、过渡句或半成品句式时使用；强制要求填入用户真实信息，禁止空话。不用于对话闲聊。"
metadata:
  version: 2.0.0
license: CC-BY-NC-SA-4.0
---

# Gongwen Gems — 强约束公文辅助句库

面向县处级机关及为上级代写场景。100 句按**使用场景**组织（非文种），每条含**必替换项**元数据，强制 AI 填入用户真实信息。

> 句库全文见 [`references/gems.md`](references/gems.md)。使用本技能前必须读取该文件。
> 完整范例见 [`examples/`](examples/)。

## ⛔ 绝对禁止事项（黑名单）

1. **禁止连续使用**：同一段落内不得出现 2 条及以上本库句子；全文不得连续堆砌本库句子。
2. **禁止直接复制**：任何句子不得原样粘贴。必须：
   - 替换 `{占位符}`（若有）；
   - 改写开头一两个词或整体用词至少 20%，使之贴合当前语境。
3. **禁止替代事实**：用户未提供具体数据、案例、时间、对象时，**不得**使用本库句子。先用用户给的素材；素材不足就向用户要，而不是用句子凑。
4. **禁止留空占位符**：`{…}` 必须填实，禁止输出带花括号的句子。
5. **禁止虚构数据**：成效数据型句式中所有数字必须来自用户材料，不得编造。
6. **禁止用本库句子成文**：本库是点睛与过渡工具，不是文章主体。

## ✅ 正确使用步骤（必须按顺序执行）

1. **先读用户材料**：提取核心目标、具体问题、真实数据、称谓、时间。
2. **判断是否必要**：仅在需要强调决心、承上启下、提炼要点时选句。没有合适位置就不选。
3. **选句并填空**：从 `references/gems.md` 按场景筛选 → 替换全部 `{占位符}` → 改写开头用词贴合语境。
4. **自我检查**（输出前逐项打勾）：

- [ ] 该句是否带有具体信息？（若无具体指向 → 弃用）
- [ ] 去掉这句话，上下文是否依然完整？（若完整 → 此句多余，弃用）
- [ ] 占位符是否全部填实？（有残留 `{` → 重写）
- [ ] 是否改写至少 20% 用词？（原样照搬 → 重写）
- [ ] 本段是否已有本库句子？（有 → 改用其他方式表达）
- [ ] 全篇用量是否超配额？（见下）
- [ ] 数据是否全部来自用户材料？（有编造 → 删除并索要素材）

## 使用配额限制

| 文稿篇幅 | 本库句子用量上限 |
|---|---|
| ≤1500 字 | 不超过 2 处 |
| 1500–3000 字 | 不超过 3 处 |
| ≥3000 字 | 不超过 4 处 |

超过配额视为滥用，应主动提醒用户并删除多余句子。

## 正反例

**❌ 错误**："我们要不忘初心，牢记使命，奋勇前行。"（直接复制，无具体内容，空洞）

**✅ 正确**："立足我区老旧小区改造任务，我们要坚持'一户一策'的工作方法，确保年底前完成 80% 的加装电梯目标。"（选用句式并填入具体任务和数据）

**❌ 错误**："面对困难，我们必须真抓实干、攻坚克难、砥砺前行。"（四词堆砌，零信息）

**✅ 正确**："针对群众反映集中的停车难问题，我们建立台账、对账销号，上半年新增公共停车位 1200 个。"（选用库内"建立台账、对账销号"句式，填入具体问题与数据）

## 免责提示（须在提供句子时告知用户）

本库仅为辅助灵感，最终文稿必须结合本地实际情况。建议用户提供更多一手素材（具体事例、数据、举措、时间节点），以确保文稿有血有肉、可落地、可核查。本库不承担代拟正式公文的责任。

## 分桶速查（按使用场景）

| 场景 | 句数 | 典型位置 |
|---|---|---|
| 开头定调 | 7 | 篇首 |
| 问题分析 | 12 | 问题段 |
| 对策措施 | 35 | 对策段 |
| 成效展示 | 8 | 成绩段 |
| 表态承诺 | 15 | 表态段/结尾 |
| 过渡衔接 | 9 | 段间 |
| 结尾收束 | 2 | 篇尾 |
| 规范用语 | 12 | 法定公文固定位置 |
